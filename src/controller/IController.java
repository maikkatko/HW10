package controller;

/**
 * This interface represents the controller interface and has the method signature for the run
 * method which the controller uses to give input to the model and record output.
 */
public interface IController {

  /**
   * Runs the image processor.
   */
  void run();
}
